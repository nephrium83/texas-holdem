__pycache__/
*.py[cod]
build/
dist/
*.egg-info/
.pytest_cache/
.venv/
venv/
# PyInstaller
*.spec.bak

# local launcher with a machine-specific interpreter path
Play Poker.bat

# native crypto libraries (build artifacts — see native/README.md to rebuild)
native/*.dll
native/*.so
native/*.dylib
native/*.lib

# benchmark run output (machine-specific; recorded results live in docs/)
benchmarks/*_result.json
